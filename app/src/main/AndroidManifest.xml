<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.wordhub">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.train.ReviewActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".ui.center.RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".ui.center.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.word.BookActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.word.LoadActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".ui.word.SearchActivity" />
        <activity android:name=".ui.word.ReciteActivity" />
        <activity
            android:name=".ui.center.LexiconActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.find.RecordActivity"/>
        <activity android:name=".ui.word.PlanActivity"/>
    </application>

</manifest>